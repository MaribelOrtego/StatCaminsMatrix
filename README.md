# StatCaminsMatrix
StatCamins. Basic matrix operations and SVD decomposition

# First steps to activate the tutorial

## Let's install the packages that we need. Installation is only done once. Package will be installed for next tutorials

#install.packages("learnr")

#install.packages("devtools")

## We activate the package (once per session if we need the package)
library("learnr")

library("devtools")

## Download the tutorial from the repository:
devtools::install_github("MaribelOrtego/StatCaminsMatrix")

## Execute the tutorial:
learnr::run_tutorial("StatCaminsMatrix", "StatCaminsMatrix")

### And now follow the tutorial. ( Always follow the Next Page buttons) 

